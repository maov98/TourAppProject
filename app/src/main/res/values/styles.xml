<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Fragment1ImageViewStyle">

        <item name="android:layout_width">@dimen/pictureIconSize</item>
        <item name="android:layout_height">@dimen/pictureIconSize</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:scaleType">centerCrop</item>

    </style>


    <style name="Fragment1TextViewStyle">

        <item name="android:layout_height">match_parent</item>
        <item name="android:minWidth">@dimen/itemListTextLength</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_weight">2</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">@dimen/normalPaddingValue</item>
        <item name="android:textColor">#FFFFFF</item>

    </style>

    <style name="Fragment1ActionButtonStyle">


        <item name="android:layout_height">@dimen/actionIconSize</item>
        <item name="android:layout_width">@dimen/actionIconSize</item>
        <item name="android:gravity">center_vertical</item>
        <item name="android:paddingLeft">@dimen/normalPaddingValue</item>
        <item name="android:layout_gravity">center_vertical</item>




    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
